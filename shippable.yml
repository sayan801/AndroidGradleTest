language: java

jdk:
   - oraclejdk7
     
install:
  - sudo apt-get install -u lib32stdc++6 lib32z1
    
script:
  - ./gradlew clean build
  
notifications:
  email:
    recipients:
      - chandra.sengupta@mycuratio.com
      - info@technicise.com
         
